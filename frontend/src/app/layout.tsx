import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Aisha CRM — Dubai National Insurance Voice Agent",
  description: "AI-powered outbound calling dashboard for Dubai National Insurance. Automated welcome calls with real-time transcription and CRM integration.",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
